export const single = (position, board) => {
  console.log("single note playing")
  //some math depending on position to determine what audio file to play
}

export const strum = (position, board) => {
  console.log("strum of chord playing")
  //some math depending on position to determine what audio file to play
}